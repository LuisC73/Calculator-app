import React from 'react';
import { render } from '@testing-library/react';
import { Button } from '../../../src/components';

describe('Firts', () => {
  test('should first', () => {
    render(<Button />);
  });
});
